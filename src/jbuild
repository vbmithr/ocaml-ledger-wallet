(jbuild_version 1)

(library
 ((name        ledgerwallet)
  (public_name ledgerwallet)
  (modules     (Apdu Transport Btc))
  (preprocess (pps (ppx_sexp_conv)))
  (synopsis "Ledger wallet library for OCaml")
  (libraries (result cstruct hidapi bitcoin))))
